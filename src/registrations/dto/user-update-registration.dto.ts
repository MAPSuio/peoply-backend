import { CreateRegistrationDto } from "./create-registration.dto";

/** Field-for-field the same contract as creating a registration. */
export class UserUpdateRegistrationDto extends CreateRegistrationDto {}
